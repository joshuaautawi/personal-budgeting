# Copy to `.env` (same folder as docker-compose.yml) if you want to override defaults.
# Docker Compose automatically loads `.env`.

POSTGRES_DB=budgeting
POSTGRES_USER=budgeting
POSTGRES_PASSWORD=budgeting


